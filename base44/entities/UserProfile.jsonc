{
  "name": "UserProfile",
  "type": "object",
  "properties": {
    "userId": {
      "type": "string",
      "description": "Base44 user id this profile belongs to"
    },
    "fullName": {
      "type": "string"
    },
    "username": {
      "type": "string"
    },
    "email": {
      "type": "string",
      "format": "email"
    },
    "phone": {
      "type": "string"
    },
    "avatar": {
      "type": "string",
      "description": "URL to profile avatar"
    },
    "role": {
      "type": "string",
      "enum": [
        "customer",
        "seller",
        "moderator",
        "admin",
        "super_admin"
      ],
      "default": "customer"
    },
    "accountStatus": {
      "type": "string",
      "enum": [
        "active",
        "suspended",
        "pending",
        "blocked"
      ],
      "default": "active"
    },
    "referredByPromoCode": {
      "type": "string"
    },
    "referralCode": {
      "type": "string",
      "description": "Permanent server-generated code owned by this user"
    },
    "referredByUserId": {
      "type": "string",
      "description": "Permanent server-owned referral relationship"
    },
    "bvn": {
      "type": "string",
      "description": "Bank Verification Number \u2014 required KYC for the customer's dedicated virtual account"
    },
    "nin": {
      "type": "string",
      "description": "National Identity Number \u2014 optional KYC for the dedicated virtual account"
    },
    "lastLoginAt": {
      "type": "string",
      "format": "date-time"
    },
    "description": {
      "type": "string",
      "maxLength": 1000,
      "description": "Optional free-form notes, up to 1000 characters"
    }
  },
  "required": [
    "userId",
    "fullName",
    "username",
    "email"
  ],
  "rls": {
    "create": false,
    "read": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "data.userId": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "update": {
      "$or": [
        {
          "data.userId": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "delete": false
  }
}